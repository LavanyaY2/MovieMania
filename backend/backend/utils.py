# import csv
# from django.conf import settings
# from pprint import pprint

# MOVIES_CSV = settings.DATA_DIR / "movies.csv"

# def load_movies_data(limit=10):
#     with open(MOVIES_CSV, newline='') as csvfile:
#         reader = csv.DictReader(csvfile)
#         for i, row in enumerate(reader):
             